ご注文受付のお知らせ

以下の内容でご注文を承りました。

----------------------------------
受付日時 : <%= @order.updated_at %>
お届け先 : <%= @order.address %>
<% @order.order_details.each do |detail| %>
  タイトル : <%= Book.find(detail.book_id).title %>
  数量 : <%= detail.quantity %>　冊
<% end %>
----------------------------------
